interface:
  display_name: "QS Code: Build"
  short_description: "Implement a specification or tracked ticket"
  default_prompt: "Use $qs-code-build to implement this specification or ticket with appropriate tests."
policy:
  allow_implicit_invocation: false
