interface:
  display_name: "QS Deploy: Release"
  short_description: "Safely verify and run a documented deployment"
  default_prompt: "Use $qs-deploy-release to verify and run this project's documented release workflow. QuickStark modes: effort=quick|standard|deep and report=brief|full; defaults are standard and brief. Model selection remains task-level."
