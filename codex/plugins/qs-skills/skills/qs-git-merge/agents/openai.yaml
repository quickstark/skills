interface:
  display_name: "QS Git: Merge"
  short_description: "Safely resolve Git merge and rebase conflicts"
  default_prompt: "Use $qs-git-merge to resolve the current merge or rebase conflicts without losing work."
