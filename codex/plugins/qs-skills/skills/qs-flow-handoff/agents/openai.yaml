interface:
  display_name: "QS Flow: Handoff"
  short_description: "Prepare a concise handoff for another session"
  default_prompt: "Use $qs-flow-handoff to prepare a concise handoff so another session can continue this work."
policy:
  allow_implicit_invocation: false
