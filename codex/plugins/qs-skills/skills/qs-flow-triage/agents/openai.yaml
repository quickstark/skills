interface:
  display_name: "QS Flow: Triage"
  short_description: "Triage incoming issues into actionable work"
  default_prompt: "Use $qs-flow-triage to triage these incoming issues into clear, actionable work."
policy:
  allow_implicit_invocation: false
