interface:
  display_name: "QS Code: Debug"
  short_description: "Reproduce and diagnose a bug or regression"
  default_prompt: "Use $qs-code-debug to reproduce, diagnose, and fix this bug with a regression test."
