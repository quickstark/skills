interface:
  display_name: "QS Plan: Specification"
  short_description: "Turn agreed requirements into a clear spec"
  default_prompt: "Use $qs-plan-spec to turn the agreed requirements into an actionable specification."
policy:
  allow_implicit_invocation: false
